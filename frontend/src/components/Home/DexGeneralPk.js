import React, { memo } from 'react';
import CardPokemon from '../CardPokemon/CardPokemon';

const DexGeneralPk = ({ poke_count, poke_list }) => {
	return <p>f</p>;
};

export default memo(DexGeneralPk);
